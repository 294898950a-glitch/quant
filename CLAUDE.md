# CLAUDE

Load `AGENTS.md` first, then load `data/research_framework/runtime_entrypoints.yaml`.

This file is only a Claude Code auto-entry pointer. The authoritative runtime
state, rules, experiments, and research memory live in machine-readable files
under `data/research_framework/`.
