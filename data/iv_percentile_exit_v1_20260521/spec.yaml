schema_version: 1
run_id: iv_percentile_exit_v1_20260521
date: '2026-05-21'
strategy_id: cb_arb_value_gap_switch
l0_entry_id: 1
l0_source: autonomous strategy ideator from recent_results_digest
hypothesis: Exiting convertible bond positions when the implied volatility percentile
  is high (indicating stress/overvaluation of option component) will protect gains
  and reduce drawdowns, thereby improving risk-adjusted returns over the baseline
  fixed exit rule.
source_insight: CB arbitrage value gap strategies often suffer when market-wide volatility
  spikes, compressing gaps or causing adverse option repricing; a volatility-based
  exit can lock in profits before those spikes.
parameter_space:
- name: mechanics
  range:
  - new_exit_rule
  type: categorical
  description: Mechanics resolved from capability_ids requested by the generated proposal.
capability_ids:
- C007
mechanics:
- new_exit_rule
new_data_sources:
- path: data/cb_arb_value_gap_switch_regime-option-entry-gate_2026-05-17/daily_value_gap_amounts.parquet
- path: data/cb_warehouse/cb_basic.parquet
- path: data/cb_warehouse/stk_daily_qfq.parquet
- path: data/cb_arb_value_gap_switch_regime-option-entry-gate_2026-05-17/baseline_trade_pnl.parquet
- path: data/cb_warehouse/cb_daily.parquet
hard_floors:
  criteria:
  - Test period total return > baseline total return
  - Test period excess return > 0
  - Test period max drawdown not worse than baseline by more than 2 percentage points
  - Test period Sharpe ratio > 0.5
  - 2020 repair year total return > -5% relative to benchmark
cv_design: single-window
cv_holdout_years:
- 2025
- 2026
compute_estimate:
  sig_minutes: 0
  spot_minutes: 0
  local_minutes: 0
  estimated_cost_yuan: 0.0
stop_conditions:
- executor match fails
- required artifacts are missing
artifacts_required:
- implementation_plan.yaml
- spec.yaml
status: DRAFT
auxiliary_metrics:
- train_excess_return
- test_excess_return
- max_drawdown
escalation:
- DRAFT requires executor implementation before run
notes: proposal required data missing
proposal:
  proposal_id: iv_percentile_exit_v1_20260521
  strategy_id: cb_arb_value_gap_switch
  family: exit_rule
  hypothesis: Exiting convertible bond positions when the implied volatility percentile
    is high (indicating stress/overvaluation of option component) will protect gains
    and reduce drawdowns, thereby improving risk-adjusted returns over the baseline
    fixed exit rule.
  source_insight: CB arbitrage value gap strategies often suffer when market-wide
    volatility spikes, compressing gaps or causing adverse option repricing; a volatility-based
    exit can lock in profits before those spikes.
  expected_improvement: Higher test-period Sharpe and lower max drawdown, while preserving
    positive excess return.
  capability_ids:
  - C007
  required_executor: cb_arb_iv_percentile_exit_executor
  required_data:
  - data/cb_arb_value_gap_switch_regime-option-entry-gate_2026-05-17/daily_value_gap_amounts.parquet
  - data/cb_warehouse/cb_basic.parquet
  - data/cb_warehouse/stk_daily_qfq.parquet
  - data/cb_arb_value_gap_switch_regime-option-entry-gate_2026-05-17/baseline_trade_pnl.parquet
  - data/cb_warehouse/cb_daily.parquet
  test_design:
    description: Conduct grid search over IV percentile threshold (e.g., 0.80 to 0.99)
      and min holding days (1 to 10). Use train period 2019-2024 to select best candidate,
      then evaluate on 2020 repair year (2020 only) and sealed test period 2025-01-01
      to 2026-05-08. Compare against baseline strategy.
  success_criteria:
    criteria:
    - Test period total return > baseline total return
    - Test period excess return > 0
    - Test period max drawdown not worse than baseline by more than 2 percentage points
    - Test period Sharpe ratio > 0.5
    - 2020 repair year total return > -5% relative to benchmark
  falsifiers:
    train:
    - Best candidate fails any success criterion on test set
    - 2020 repair year total return underperforms baseline by more than 5 percentage
      points (return delta < -0.05)
    validate:
    - Best candidate fails any success criterion on test set
    - 2020 repair year total return underperforms baseline by more than 5 percentage
      points (return delta < -0.05)
    test:
    - Best candidate fails any success criterion on test set
    - 2020 repair year total return underperforms baseline by more than 5 percentage
      points (return delta < -0.05)
  risk: IV estimation for convertible bonds may be noisy; percentile calculation may
    be lagging; may miss profitable continuations after volatility spikes.
  why_not_repeated_failure: Previous exit rule runs (dynamic exit, profit-taking,
    adaptive sell gap) either produced unusable numeric outputs due to implementation
    bugs or were rejected based on limited criteria; the IV-percentile exit is a distinct
    market-regime-based exit not previously tested, and it does not overlap with closed
    families like option_position_sizing or regime_classifier_variant.
  related_prior_runs:
  - cb_arb_value_gap_switch_dynamic_exit_2026-05-19
  - cb_arb_value_gap_switch_exit_dynamic_trail_20260519
  - cb_arb_value_gap_switch_profit-taking-exit_2026-05-19
  - cb_arb_value_gap_switch_adaptive_sell_gap_2026-05-19
  implementation_assumption: Use daily CB implied volatility approximated from Black-Scholes
    using stock volatility as proxy or direct CB pricing model; compute 252-day rolling
    percentile; exit if current IV percentile exceeds threshold.
  ai_provider: deepseek
  prompt_path: framework/autonomous/strategy_ideator.py
  response_hash: 247344cd9d8f1371
  provenance:
    ai_provider: deepseek
    prompt_path: framework/autonomous/strategy_ideator.py
    response_hash: 247344cd9d8f1371
  rewrite_status: disabled_two_step_flow
  rewrite_rounds_used: 0
  rewrite_last_errors: []
  proposal_path: /home/jay/projects/quant/data/iv_percentile_exit_v1_20260521/proposal.yaml
  mechanics:
  - new_exit_rule
automation_skip:
  status: skipped_non_runnable
  at: '2026-05-22T06:57:58'
  proposal_status: DRAFT
  reason: proposal required data missing
  errors: []
